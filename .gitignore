.DS_Store

# Internal workspace notes archived under docs/archive/ (not meant to ship)
docs/archive/00_MASTER_COORDINATOR.md
docs/archive/01_CLOUD_PROD_READY.md
docs/archive/02_LLM_BRIDGE.md
docs/archive/03_DIARY_UPDATES.md
docs/archive/CORNELL_DIARY_CLAUDE_CODE_PROMPT.md
docs/archive/parser.md
new mds.zip

# Build artifacts are committed to GitHub Releases, not into the repo
*.apk
*.aab
*.dmg
*.app

# SQLite snapshots taken before destructive migrations.
# Tracked locally so we can restore but never pushed (binary, PII).
backups/

# Sibling project with its own GitHub repo (DenizTanisman/journal-ai-reporter).
# Lives inside this directory for convenience but is not part of this repo.
journal_ai_reporter/
